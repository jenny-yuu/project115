# RAG 技術架構說明文件 - 台鐵智慧行程助理

本文件詳細說明「台鐵智慧行程助理」中 **檢索增強生成 (RAG)** 的實作細節，包括資料收集、非結構化資料處理、向量化以及檢索逻辑。

## 1. 資料來源 (Data Sources)
系統整合了多維度的防災與交通資料：
- **歷史災害紀錄**：包含歷年颱風、地震及重大鐵路事故的處置報告（地點、災情、恢復時間、解決方案）。
- **官方 SOP 專家知識**：將台鐵內部的災害應變手冊（如地震隨震度調整的巡軌規範）轉為結構化 JSON。
- **即時氣象資料 (CWA API)**：雨量、風速、溫度的實測與預報。
- **交通接駁資料 (TDX API)**：公車即時班次與官方轉乘資訊。

## 2. 非結構化資料處理 (Data Ingestion)
- **清洗與分塊**：將長篇事故報告依據「事件類型」與「地理位置」進行分塊 (Chunking)，確保檢索的精確度。
- **特徵提取**：提取關鍵字（如：土石流、斷軌、巡軌時間）作為元資料 (Metadata)。

## 3. 向量化與存儲 (Vectorization & Storage)
- **Embedding 模型**：使用 OpenAI 的 `text-embedding-3-small` 模型，將文字轉為 1536 維的向量。
- **向量資料庫**：採用 **Pinecone** 作為向量索引伺服器，支援高效的語義相似度搜尋。
- **索引結構**：
    - `Namespace`: `disaster-rag`
    - `Metadata`: 包含 `category`, `location`, `situation`, `solution`, `recovery_time` 等欄位，方便在檢索後進行後處理。

## 4. 檢索與生成流程 (Retriever & Generator)
1. **輸入處理**：接收使用者查詢或系統觸發的災害警報。
2. **語義搜尋**：將查詢轉為向量，在 Pinecone 中尋找 Top-K (通常為 5 筆) 最相關的歷史案例。
3. **上下文整合**：
    - 將檢索到的歷史案例。
    - 結合 `expert_knowledge.json` 中的硬性規則 (SOP)。
    - 合併即時交通與氣象數值。
4. **AI 決策/生成**：使用 `gpt-4o-mini` 模型，以 JSON 格式產出總結、安全建議、路徑規畫與預估恢復時間。

## 5. 優化機制 (Optimizations)
- **保底機制**：當 RAG 檢索不到相似案例時，系統會調用專家 SOP (expert_knowledge) 進行保底推估。
- **動態 Prompt**：根據「是否停駛」調整 AI 建議的重點（例如：停駛時重點在接駁計算與避難）。
